#!/usr/bin/python3
"""inheritance from list"""


""" check if its the same class """
        def is_kind_of_class(obj, a_class):
                if type(obj) == a_class:
                        return True
                return False
